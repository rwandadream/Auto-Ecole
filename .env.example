# ============================================================
# Variables d'environnement SARAH AUTO
# Copiez ce fichier en .env.local et renseignez vos valeurs
# ============================================================

# Supabase — clés publiques (côté client, préfixe NEXT_PUBLIC_ requis)
NEXT_PUBLIC_SUPABASE_URL=https://VOTRE_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_VOTRE_CLE_PUBLIQUE

# Supabase — clé service role (côté serveur uniquement, ne jamais exposer au client)
# Requise pour les routes /api/admin/users (création / suppression d'utilisateurs)
SUPABASE_SERVICE_ROLE_KEY=VOTRE_SERVICE_ROLE_KEY

# ============================================================
# Super Administrateur — compte initial (SEED SQL UNIQUEMENT)
# Ces variables ne sont PAS lues par l'application au runtime.
# Exécutez supabase/seed_superadmin.sql dans le SQL Editor Supabase
# en remplaçant v_email et v_password par les valeurs ci-dessous.
# ============================================================
SUPERADMIN_EMAIL=superadmin@sarahauto.ci
SUPERADMIN_PASSWORD=SuperAdmin2026!
